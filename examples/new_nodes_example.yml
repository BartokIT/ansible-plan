defaults:
  inventory: inventory.ini
workflow:
  - id: start
    block:
      - id: my_label
        label: true
        description: "This is a label node."
      - id: my_checkpoint
        checkpoint: true
        description: "This is a checkpoint node. Please confirm to proceed."
      - id: say_hello
        import_playbook: playbooks/first.yml
        reference: "hello"
  - id: end
    block: []
